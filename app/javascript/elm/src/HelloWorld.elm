module HelloWorld exposing (..)

import Html exposing (Html, span, text)
import Html.Attributes exposing (class)


main : Html msg
main =
    span [ class "font-mono text-gray-300 text-sm" ] [ text "Replace this with your Elm component." ]
